<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="min-h-screen p-10 bg-zinc-900 text-white font-sans">
      
      
      <div class="w-full flex justify-end">
        <a
          class="px-4 py-2 bg-red-500 hover:bg-red-600 transition rounded-md text-sm mb-6"
          href="/logout"
        >
          Logout
        </a>
      </div>

      <h5 class="mb-6 text-zinc-400">Edit Your Post</h5>

      
      <form action="/update/<%= post._id %>" method="post" class="mb-20">
        <textarea
          class="resize-none block outline-none w-1/3 p-3 bg-zinc-800 border border-zinc-700 rounded-md focus:border-blue-500"
          name="content"
          placeholder="What's on your mind?"
        ><%= post.content %></textarea>

        <input
          type="submit"
          class="block text-sm w-40 px-3 py-2 text-black bg-yellow-500 hover:bg-yellow-600 transition rounded-md mt-3"
          value="Update Post"
        />
      </form>

    </div>
  </body>
</html>
