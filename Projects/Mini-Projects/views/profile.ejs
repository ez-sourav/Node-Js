<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="min-h-screen bg-zinc-900  text-white font-sans px-6 py-10">
      
      <!-- Top Bar -->
      <div class="w-full flex justify-end mb-8">
        <a
          class="px-5 py-2 bg-red-500/90 hover:bg-red-600 transition rounded-lg text-sm font-medium shadow"
          href="/logout"
        >
          Logout
        </a>
      </div>

      
      <div class="flex items-center gap-4 mb-2">
        <div class="w-12 h-12 rounded-full overflow-hidden ring-2 ring-blue-500/40">
          <img
            class="w-full h-full object-cover"
            src="/images/uploads/<%= user.profilepic %>"
            alt="profile"
          />
        </div>
        <h1 class="text-2xl md:text-3xl font-semibold">
          Hello, <span class="text-blue-400"><%= user.name %></span> 👋
        </h1>
      </div>

      <p class="mb-8 text-zinc-400">
        You can create a post below
      </p>

      
      <form action="post" method="post" class="mb-20">
        <textarea
          class="resize-none block outline-none w-full md:w-1/2 p-4 bg-zinc-800 border border-zinc-700 rounded-xl focus:border-blue-500 focus:ring-2 focus:ring-blue-500/30 transition"
          name="content"
          placeholder="What's on your mind?"
        ></textarea>

        <input
          type="submit"
          class="mt-4 w-44 px-4 py-2 bg-blue-500 hover:bg-blue-600 transition rounded-xl text-sm font-semibold shadow"
          value="Create New Post"
        />
      </form>

     
      <div class="posts">
        <h3 class="text-zinc-400 text-lg mb-6">
          Your Posts
        </h3>

        <div class="postcontainer space-y-6">
          <% if (user.posts.length > 0) { %>
          <% user.posts.reverse().forEach(post => { %>

          <div
            class="post w-full md:w-1/2 p-5 rounded-2xl border border-zinc-700 bg-zinc-800 shadow hover:shadow-lg transition"
          >
            <div class="flex items-center gap-3 mb-3">
              <img
                class="w-10 h-10 rounded-full object-cover ring-1 ring-zinc-600"
                src="/images/uploads/<%= user.profilepic %>"
                alt="profile"
              />
              <h4 class="text-lg font-semibold text-blue-400">
                @<%= user.username %>
              </h4>
            </div>

            <p class="text-sm text-zinc-200 leading-relaxed mb-3">
              <%= post.content %>
            </p>

            <small class="text-zinc-400 block mb-2">
              <%= post.likes.length %> likes
            </small>

            <div class="btns flex gap-6 text-sm">
              <a class="text-blue-500 hover:text-blue-400 transition" href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
              </a>
              <a
                class="text-zinc-400 hover:text-zinc-200 transition"
                href="/edit/<%= post._id %>"
              >
                Edit
              </a>
            </div>
          </div>

          <% }) %>
          <% } else { %>

         
          <div
            class="w-full md:w-1/2 p-8 text-center border border-dashed border-zinc-700 rounded-2xl bg-zinc-800"
          >
            <h1 class="text-lg font-semibold mb-2">
              No posts yet
            </h1>
            <p class="text-zinc-400 text-sm">
              Create your first post to get started 🚀
            </p>
          </div>

          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
