<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="min-h-screen bg-zinc-900 text-white font-sans px-6 py-10">
      
 
      <div class="w-full flex justify-end mb-8">
        <a
          class="px-5 py-2 bg-red-500/90 hover:bg-red-600 transition rounded-lg text-sm font-medium shadow"
          href="/logout"
        >
          Logout
        </a>
      </div>

     
      <h5 class="mb-6 text-zinc-400 text-lg font-medium">
        Edit Your Post
      </h5>

      
      <form action="/update/<%= post._id %>" method="post" class="mb-20">
        <textarea
          class="resize-none block outline-none w-full md:w-1/2 p-4 bg-zinc-800 border border-zinc-700 rounded-xl focus:border-yellow-500 focus:ring-2 focus:ring-yellow-500/30 transition"
          name="content"
          placeholder="What's on your mind?"
        ><%= post.content %></textarea>

        <input
          type="submit"
          class="mt-4 w-44 px-4 py-2 bg-yellow-500 hover:bg-yellow-600 transition rounded-xl text-sm font-semibold text-black shadow"
          value="Update Post"
        />
      </form>

    </div>
  </body>
</html>
